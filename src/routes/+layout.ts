// import type { FieldName } from '$types'
// import focus from '$lib/focus'

export async function load({ data, url }) {
	// const focusedFieldName = url.searchParams.get('f') as FieldName | null

	// const preferredFocus = focusedFieldName ?? 'all the things'

	// focus.set(preferredFocus)
	// url.searchParams.delete('f')

	return data
}
