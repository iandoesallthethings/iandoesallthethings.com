// import type { FieldName } from '$types'
// import focus from '$lib/focus'

export async function load({ data, url }) {
	// const fieldNameToFocus = url.searchParams.get('f') as FieldName | null

	// if (fieldNameToFocus) {
	// 	focus.set(fieldNameToFocus)
	// }

	return data
}
